<UserControl x:Class="AttendanceMarker.Views.Dashboard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AttendanceMarker.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Background="#ffcd64">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Image Grid.Column="0" Margin="20">
                <Image.Source>
                    <BitmapImage UriSource="/Resources/logo.png" />
                </Image.Source>
            </Image>
                <TextBlock Text="Date" Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                <Button Content="ShutDown" Grid.Column="2">
                    
                </Button>
        </Grid>
    </Grid>
</UserControl>
